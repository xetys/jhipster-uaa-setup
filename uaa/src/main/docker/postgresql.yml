version: '2'
services:
    uaa-postgresql:
        container_name: uaa-postgresql
        image: postgres:9.5.1
        # volumes:
        #     - ~/volumes/jhipster/UAA/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=UAA
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
